---
description: "Update the active skill profile with currently enabled skills."
---
Use the bash tool to run the following command and display its output:

```bash
"$HOME/.copilot/plugin-data/skills-profile/run.sh" update
```
