---
description: "List all skill profiles, marking the active one."
---
Use the bash tool to run the following command and display its output:

```bash
"$HOME/.copilot/plugin-data/skills-profile/run.sh" list
```
